import "../GlobalEvent/GlobalEvent.css";

import { Button } from "react-bootstrap";

const EventButton = () => {
  return (
    <Button variant="outline-primary" id="EventButton">
      Apply
    </Button>
  );
};

export default EventButton;